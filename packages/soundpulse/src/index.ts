/**
 * SoundPulse — Web Audio API synthesized sound engine.
 * All 21 sounds are generated via oscillators — zero audio files.
 * Respects system mute and user volume preference.
 */

export { SoundEngine } from './engine';
export { SOUND_EVENTS, type SoundEventKey } from '@ymshots/types';
